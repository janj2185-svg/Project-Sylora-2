# SYLORA — working core v0.1

This is the first runnable vertical slice of SYLORA. It deliberately implements fewer things for real instead of pretending the entire long-term platform already exists.

The canonical visual direction is defined in [SYLORA Canonical Product Design](docs/SYLORA-CANONICAL-DESIGN.md) and anchored by the user-approved reference at `docs/design/sylora-canonical-ui-reference.png`. New tabs and redesign work must remain in that single light, luminous SYLORA design language across desktop, tablet and mobile.

## What actually works

- registration/login/logout with scrypt password hashing and opaque bearer sessions
- editable profile and UK/PL/EN locale preference
- persistent social feed, posts, reactions, comments and follows
- notifications generated by interactions
- wallet with starter test balance, gift catalog and append-only gift ledger entries
- server-sent realtime gift events and a first animated browser gift renderer
- direct conversations and persisted messages
- communities with membership, channels and channel posts
- free-course publishing, protected lesson content, enrollment and per-lesson/course progress (paid enrollment fails safely until payments exist)
- business profiles and cross-domain search
- LIVE room lifecycle and chat control plane (media transport intentionally separate)
- creator statistics endpoint
- realtime per-room LIVE event stream (presence, viewer count, chat and gift event fanout)
- Redis Pub/Sub LIVE event fanout for cross-instance chat/gift/overlay/control-plane delivery with per-instance echo deduplication; Redis leases also coordinate peer ownership and expiring viewer presence/counts across instances, with local development fallback
- PostgreSQL transactional realtime outbox for gift events: the durable event is committed with the wallet/ledger transfer, claimed safely by workers, retried with backoff, published across Redis, and deduplicated by stable IDs in clients/OBS effects
- browser Creator Studio: camera/mic, screen share, branded composition and MediaRecorder WebM recording with vertical 720×1280@30, vertical 1080×1920@30/60 and landscape 1920×1080@30 output profiles
- Web Audio Studio mixer with live level meter, 0–150% source gain and mute; processed audio is routed into both MediaRecorder output and WebRTC LIVE instead of being a decorative control
- early real WebRTC P2P broadcast path: viewer discovery, offer/answer/ICE signaling, Studio canvas+audio tracks and viewer playback
- hardened P2P signaling lifecycle: authenticated Redis-coordinated peer ownership, explicit host/viewer registration, viewer-left cleanup, host-side six-peer safety cap and explicit viewer rejection when that development P2P limit is reached
- validated authenticated WebRTC ICE configuration shared by Studio host and viewers; optional `SYLORA_ICE_SERVERS_JSON` enables configured STUN/TURN relay candidates without hard-coding credentials in the client bundle
- server-persisted Creator Studio scene presets plus local logo/image overlay sources rendered into recording and LIVE composition
- direct local OBS WebSocket 5.x client with challenge/salt authentication, capability discovery, program-scene switching, Virtual Camera and OBS Stream start/stop controls plus bounded exponential reconnect after unexpected disconnects; stream service/key configuration remains inside OBS and the OBS password stays in browser memory rather than being sent to the SYLORA API
- loopback-only SYLORA Companion bridge for Creator Studio: pairing-token authentication, explicit browser-origin allowlist, bounded JSON requests and a small OBS action allowlist; Studio can route scene/Virtual Camera/stream controls through the local companion so OBS credentials remain on the creator PC
- recoverable Creator Studio working state plus server scene presets for output profile, overlay style/title and microphone gain/mute; selected presets can be updated in place instead of duplicating a scene on every save
- host-only, expiring (2 hour) OBS Browser Source URLs for a selected SYLORA LIVE; the transparent overlay receives that room's chat and gift events over a dedicated SSE channel without counting itself as a viewer
- Gift Engine V2 procedural renderer: tiered particles/shockwaves, Legendary fullscreen phoenix scene and generated Web Audio sound
- gift combos up to ×99 with server-side gross/creator/platform calculations and tracked creator earnings
- real local MP4/WebM upload with size/type/signature validation
- FFprobe video metadata (duration/dimensions), persistent media ownership and SHA-256
- HTTP Range media delivery for seekable playback
- publishable vertical SYLORA Clips feed with a browser uploader/player
- asynchronous FFmpeg HLS job pipeline with queued/processing/ready/failed states and progress
- authenticated transcode/status API plus validated `.m3u8`/segment delivery
- responsive, light futuristic SYLORA web shell
- health endpoint and official OpenAI JavaScript SDK integration
- same-origin security headers and basic per-IP API/auth rate limiting
- privacy-safe public user serializer (email/role/password data never leaks through public profile payloads)
- SHA-256 server-side session-token storage (new bearer tokens are never persisted in plaintext)
- authenticated per-user realtime event stream for notifications and direct-message delivery without tokens in URLs
- user blocking wired into feed/follows/direct conversations
- report queue, admin moderation state changes and audit log with admin identity configured by environment
- authenticated SYLORA AI chat backed by the OpenAI Responses API, with per-user history and AI-specific rate limiting
- OpenAI Realtime WebRTC voice conversations with live captions and voice turns unified into the same per-user AI history
- client-side SYLORA Motion Rig with spring-damped shoulder/elbow/wrist targets, state-aware breathing, voice energy, gaze and hair inertia; the Digital Human now uses an arm-free photoreal torso plus independent left/right upper arms, forearm sleeves and hands driven through the shoulder → elbow → wrist spring chain
- SYLORA AI controlled memory: user-visible memories can be added/removed, while AI-proposed memories require confirmation
- Responses API function tools with an explicit write boundary: context reads are scoped to the authenticated user; post/memory writes become expiring pending actions and execute only after user confirmation
- PostgreSQL connection + checksum-tracked atomic migration runner and dependency readiness checks
- Redis-backed distributed API/auth/AI rate limiting when `REDIS_URL` is configured, with local in-memory fallback for development
- PostgreSQL runtime repositories for registration/login, hashed sessions, profiles, social/messaging, LUMEN wallet/gift ledger, AI chat/voice history, controlled memories/pending AI actions and LIVE room/chat lifecycle when `DATABASE_URL` is configured; JSON remains the dev/test fallback
- idempotent gift transfers with deterministic wallet row locking, spendable/earnings buckets, balanced creator/platform accounting and the recipient notification committed in the same transaction

## Intentionally not faked

AI uses a real OpenAI provider when `OPENAI_API_KEY` is configured and fails explicitly when it is not. Payments, object storage/CDN, production SFU/RTMP streaming, a managed ephemeral TURN credential service, creator payouts, native apps and large-scale infrastructure are not implemented in this slice. The current WebRTC transport is deliberately P2P; external STUN/TURN can be configured, and the current media/HLS pipeline is local-server based.

OBS control is real and deliberately restricted to `localhost` / `127.0.0.1`. Creator Studio now includes a local Companion route in addition to direct development-time OBS WebSocket access. The Companion binds only to `127.0.0.1`, requires its generated pairing token, checks an origin allowlist and holds the OBS password only in process memory. Browser Source itself works as a normal OBS browser page and does not require exposing the OBS WebSocket password. Native installer/signing and automatic Companion updates are still future release-engineering work and are not claimed complete.

The regenerated arm/wrist assets passed local anatomy/alpha separation checks and their independent sprite layers are enabled in the web avatar. Shoulder, elbow and wrist transforms are rendered as a nested kinematic chain. Each hand now has four anatomy-preserving pose frames (neutral, open, conversational curl and emphasis) selected by the semantic gesture system, with a short cross-fade between frames so finger posture changes naturally together with Sylora's spring-driven arm movement. This avoids assembling a hand from disconnected finger sprites. It is pose-based finger articulation, not a per-finger skeletal rig. Exact browser/device shoulder/wrist/hand alignment still needs visual-regression QA on the target browsers before the Digital Human phase can be marked complete.

The migration is intentionally hybrid for now. PostgreSQL is authoritative for auth/users/sessions, posts, the core social/messaging graph, the test LUMEN wallet/gift ledger, AI messages/memories/actions, LIVE room/chat lifecycle, and the gift realtime outbox when configured. Redis Pub/Sub distributes LIVE and committed gift events across instances, Redis leases coordinate WebRTC peer ownership, and expiring sorted-set leases aggregate viewer presence/counts. The gift outbox uses at-least-once delivery with stable-ID client deduplication, so a process/Redis failure cannot silently erase the durable event after the financial commit. If Redis is unavailable these coordination paths fall back to one process. Redis is not required to boot; production boot requires a valid `DATABASE_URL`, and missing Redis is reported as a scale-out `DEGRADED` diagnostic. See `docs/operations/ENVIRONMENT.md` and `docs/operations/PRODUCTION_READINESS.md`. The media tracks are still deliberately P2P (six-peer Studio safety cap), not an SFU. Communities/learning, business, media metadata and moderation still use the JSON store. LUMEN remains test currency: real payments, withdrawals and creator payouts are deliberately not enabled.

## Run

Requires Node.js 22+.

For local AI, put `OPENAI_API_KEY` in `.env.local`; that file is ignored by Git. `OPENAI_MODEL` defaults to `gpt-5.6` and can be overridden there.

For WebRTC across NAT/mobile networks, configure one or more STUN/TURN servers as JSON. TURN credentials are delivered only to authenticated SYLORA users through the runtime RTC config endpoint. Production should use short-lived TURN credentials rather than permanent shared secrets.

```env
SYLORA_ICE_SERVERS_JSON=[{"urls":"stun:stun.example.com:3478"},{"urls":"turns:turn.example.com:5349","username":"ephemeral-user","credential":"ephemeral-credential"}]
```

```bash
npm start
```

Open http://localhost:8787

Or:

```bash
docker compose up --build
```

The Compose stack starts SYLORA + PostgreSQL + Redis, waits for dependency health, runs the database migration before the app starts, and uses persistent volumes. Override the development-only `POSTGRES_PASSWORD` before any non-local deployment.

## Test

```bash
npm test
```

## Gift Runtime V2

Premium gift production is governed by [SYLORA Gift Design Bible](docs/SYLORA-GIFT-DESIGN-BIBLE.md). The existing gift renderer is frozen as the Legacy V1 compatibility path while the V2 cinematic runtime is built and validated.

The V2 foundation lives in `public/gift-v2/` and starts from story beats and semantic physical events rather than a single normalized animation-progress function. It includes StoryGraph, ShotDirector, deterministic fixed-step physics, four-plane stream composition, a WebGL depth compositor, causal HRTF physical-audio playback, adaptive quality protection and automated anti-template/no-dead-time validation. The V2 design catalog now contains 20 deliberately distinct cinematic gift passports; production animation begins only after the runtime foundation passes its playback gates.

## Core API

- `GET /api/health`, `GET /api/ready`, and `GET /api/ai/status`
- `POST /api/auth/register`
- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET/PATCH /api/me`
- `GET /api/feed`
- `POST /api/posts`
- `POST /api/posts/:id/react`
- `GET/POST /api/posts/:id/comments`
- `POST /api/users/:id/follow`
- `GET /api/notifications`
- `GET /api/gifts`
- `POST /api/gifts/send`
- `GET /api/gifts/stream` (SSE)
- `GET /api/ledger`
- `GET /api/search?q=`
- `GET/POST /api/conversations` and conversation messages
- `GET/POST /api/communities`, join, channels and channel posts
- `GET/POST /api/courses`, lessons, publish, enroll and lesson progress
- `GET/POST /api/businesses`
- `GET/POST /api/live`, LIVE chat and end
- `GET /api/live/:id/engagement`, `POST /api/live/:id/like`, `POST /api/live/:id/resonance`
- `GET /api/progress` (Orbit giver progression; per-creator Bond grows from gifts)
- `GET/POST /api/conferences`, private invitations/membership, participant list and owner-controlled Sylora presence
- `GET /api/stats`
- `POST /api/media/upload` (raw MP4/WebM body, authenticated, max 100 MB)
- `GET/POST /api/videos`
- `GET /media/:id` with Range support
- `POST /api/media/:id/transcode`
- `GET /api/media/jobs/:id`
- `GET /hls/:mediaId/index.m3u8` and generated segments
- `GET /api/ai/history`
- `POST/DELETE /api/ai/memory`
- `POST /api/ai/actions/:id/confirm` and `/cancel`
- `POST /api/ai/chat` (OpenAI Responses API)
- `POST /api/ai/realtime` (authenticated WebRTC SDP exchange)
- `POST /api/ai/realtime/transcript` (idempotent final voice-turn persistence)

## Next production slice

1. Move communities/learning and the remaining mutable application domains to PostgreSQL.
2. Add durable object storage/CDN and a persistent media-job queue instead of local media/HLS artifacts.
3. Keep hardening distributed LIVE coordination. Redis now covers application fanout, peer ownership and expiring viewer presence, but P2P media itself still has a six-peer safety cap and is not an SFU.
4. Add adversarial AI/tool evals and expand the permissioned tool catalog only after each write action has an approval policy.
5. Replace the P2P LIVE transport with production SFU/TURN/RTMP infrastructure.

## Long-term capability architecture

The fourteen future-facing product capabilities are now reserved as explicit architecture contracts rather than disconnected feature ideas. Their canonical roadmap is [SYLORA — 14 Capability Architecture Roadmap](docs/SYLORA-14-CAPABILITY-ROADMAP.md), with machine-testable capability/event registration in `src/platform-vision.mjs`.

Registration is intentionally not a completion claim. The immediate proof remains a vertical slice that connects Phoenix Rebirth on Gift Runtime V2 to real LIVE composition, a Living Sylora reaction, an AI Director cue and a Living World reaction. Only after that slice passes visual, runtime and performance gates should the wider capability set be implemented in parallel product horizons.

## Ecosystem core (Human + Personal AI + Identity + Knowledge + Economy + Developers)

SYLORA is being rebuilt around one product class — an AI ecosystem — not a pile of social clones. Audit and status live in:

- [CURRENT_STATE](docs/audit/CURRENT_STATE.md)
- [IMPLEMENTATION_PLAN](docs/audit/IMPLEMENTATION_PLAN.md)
- [FINAL_IMPLEMENTATION_REPORT](docs/FINAL_IMPLEMENTATION_REPORT.md)
- [SYLORA Protocol (future)](docs/SYLORA-PROTOCOL.md)

Working foundation modules under `src/ecosystem/` expose Identity, Personal AI dashboard/permissions/activity, permission-aware Knowledge Graph, Action Engine levels, Agent Marketplace, Developer apps/API keys, translation plane, Business OS orgs + Enterprise AI Control Plane, reputation, provenance, sandbox commerce, and AI Creator Studio planning inside the existing Studio surface.

The Digital Human avatar is reassembled from the coherent white-suit gesture sheet (mismatched pink sleeve tubes are disabled) so Sylora appears whole and can gesture normally.
