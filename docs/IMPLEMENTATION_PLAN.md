# IMPLEMENTATION_PLAN

## Stage 0 — Audit
Produce CURRENT_STATE / ARCHITECTURE_AUDIT / MISSING_FEATURES / SECURITY_AUDIT / TECH_DEBT / this plan. Then implement.

## Stage 1 — Core (this cycle priority)
1. Fix cohesive Digital Human (Sylora assembled + motion).  
2. Permissions architecture + Personal AI permission dashboard.  
3. Memory tiers (short/long) + export/delete + activity log.  
4. Identity privacy levels + extended identity fields.  
5. Permission-aware Knowledge Graph foundation.  
6. Universal Action Engine with confirmation levels.  
7. Audit logs for AI/graph/agent actions.

## Stage 2 — Agent + Developer Platform
Agent manifests, install/uninstall, sandbox flags, marketplace listings, API keys, OAuth scopes registry, webhooks skeleton, JS/Python/Dart SDK stubs, developer console API.

## Stage 3 — Communication
Translation layer (detect/translate/subtitle/chat) with provider adapters; STT/TTS/voice-preserve marked BLOCKED without keys; integrate into messages + LIVE event contracts.

## Stage 4 — Creator
AI Creator Studio proposals wired into existing Studio/LIVE (no dead tab); commerce sandbox for subscriptions/products/courses; creator analytics extensions.

## Stage 5 — Business
Organizations, teams, RBAC, company knowledge, shared agents, Enterprise AI Control Plane (allow/deny, budgets, kill switch, approval policies).

## Stage 6 — Trust
Moderation queue enrichment, security center, provenance records, reputation engine with explainability + dispute hooks.

## Stage 7 — Scale
Observability metrics, AI cost/quotas, caching hooks, search (structured + semantic adapter), protocol architecture docs only.

## Definition of Done (per feature)
UI + backend + persistence + authz + loading/empty/error + desktop/mobile + tests + no critical TODO. PARTIAL/BLOCKED must never be labeled DONE.

## Network-effect check
Each module must increase value when users, creators, developers, businesses, or agents join — otherwise deprioritize.
