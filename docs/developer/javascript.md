# JavaScript / TypeScript SDK

Planned SDK surface: authenticated API client, scoped app registration, webhook signature verification and sandbox helpers. No published SDK package exists yet. Use the HTTP API directly and treat this file as a documentation pointer, not an installable stub.
