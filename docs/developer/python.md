# Python SDK

A Python SDK is planned for app authentication, scoped API calls and webhook verification. It is not published. Current integrations must use the documented HTTP contracts and sandbox credentials.
