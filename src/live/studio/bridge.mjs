/** Bridge notes — Creator Studio (renderStudio) remains the capture/OBS surface. */
export function studioBridgeNote() {
  return {
    view: 'studio',
    commandCenterView: 'liveStudio',
    note: 'Command Center orchestrates AI/platforms; Studio owns camera canvas + OBS.'
  };
}
